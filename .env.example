# Vite bakes this in at BUILD time — after setting/changing it in Vercel's
# Environment Variables, you must trigger a redeploy for it to take effect.
# Point this at your deployed backend (server/), NOT at this app's own URL.
VITE_API_BASE=https://your-backend.onrender.com

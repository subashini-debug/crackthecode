// Point this at wherever the server (from /server) is running.
// If you open these HTML files directly from the same machine as the
// server during testing, the default below (localhost:4000) works.
// For a real event, replace with your server's LAN/public URL, e.g.
// const API_BASE = "http://192.168.1.20:4000";
const API_BASE = "http://localhost:4000";

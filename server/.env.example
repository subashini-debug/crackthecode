PORT=4000
ADMIN_KEY=change-me-before-event
# Optional: you can also paste your Gemini API key directly in the Admin dashboard UI
# instead of putting it here. If set here, you can still override it per-request in the UI.
GEMINI_API_KEY=
